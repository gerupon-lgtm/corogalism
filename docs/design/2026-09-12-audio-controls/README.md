# サウンド操作の配置案（確認用）

ユーザーから音の実装中に「先にコントロール配置の画像」と依頼があり、組み込みを中断して作成。
現在のHTML/CSSへブラウザ内だけで仮のコントロールを追加した、実寸のモックアップ。

- `01-game-controls.png`: 開発中注記の右に音ON/OFF。ポーズ位置は現行と同じ。
- `02-settings-controls.png`: 最大傾き角の下へサウンドカード。音ON/OFF、BGM 60%、SE 80%。
- `03-sound-panel.png`: サウンドカードの拡大。
- 画像は音ONの状態。最初はOFFとする案。

音源は試聴v2で承認済み。UI配置はまだ確認前で、本番へ公開していない。
音の基盤は `src/audio/` と `assets/audio/` に作業途中のファイルがあるが、main.jsへは未接続。
次の作業は配置のフィードバックを反映し、イベント連動・保存・テスト・公開を完了すること。

## 配置案2

ユーザー指示で音ONボタンをポーズの左に並べた。`04-game-controls-inline.png`。検証中注記がなくなった将来画面を想定し、画像用DOMだけで注記を非表示にしている。本番の注記・noindexは変更していない。以後のプレイ画面配置はこの案を基準にする。
