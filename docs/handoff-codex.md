# Codexへの引き継ぎ（2026-09-12 更新）

**v0.4.10（T-239）公開確認済み・2506bd9:** クリア直後とコンティニュー確定時にも自己ベストを保存し、途中の再読み込みで成績が失われる経路を修正。既存記録は保持。詳細docs/run-record-checkpoints.md。

**v0.4.9（T-238）公開確認済み・f5c5ac6:** ゴム反発0.9・ゴム自体は無傷、反発先の壁で通常ダメージ。満タンキャンディは案内表示して残す。説明も更新。詳細docs/rubber-and-candy.md。

**v0.4.8（T-237）公開確認済み・62b63be:** 画面高に合わせて枠内へ余白を配分、プラクティスも対象。壁凡例を削除しPAUSEからガイドを開ける。閉じても停止維持。詳細docs/screen-fit.md。

**v0.4.7（T-236）公開確認済み・fbaa3a8:** トップの余白を整理してコピーライトを枠内へ。ガイドと設定を横並び、設定・結果の余白も調整。プレイ寸法を維持。詳細docs/compact-layout.md。

**v0.4.6（T-235）公開確認済み・f37ede1:** ガイドの葉っぱとタイトルを中央揃え。正式URLの3幅で確認済み。

**v0.4.5（T-234）公開確認済み・0de4e79:** トップにあそびのガイドを追加。壁・アイテム・床を1枚のオーバーレイに掲載。とりもち説明を指定の一文へ要約し、ひとやすみと等高。仕様docs/play-guide.md。

**v0.4.4（T-233）公開確認済み・78b4c39:** トップの設定下に手動更新チェックを追加。最新版・更新あり・通信失敗を表示し、更新適用は既存の明示操作。詳細docs/manual-update.md。

**v0.4.3（T-232）公開確認済み・9624df3:** やさしい9面から制限時間に段階的に最大12秒加算。木枠の砂時計を9面以降に抽選配置、取得+5秒。ゴム壁の実効反発を0.7へ強化。仕様はdocs/time-and-hourglass.md。

**v0.4.2（T-231）公開確認済み・1f00540:** とりもちを自然脱出3秒、ダブルタップ1組につき0.5秒短縮、最短0.5秒へ調整（下限到達には5組）。盤面内の2タップ位置合わせを不要にし、指の移動許容は48px。ヒントは球を避けて配置し、短縮成功を明示。109テストと実タッチ模擬・3画面幅を確認。詳細はdocs/leaf-floors-pwa.md。

**v0.4.1（T-230）公開確認済み・f896663:** 画面のダブルタップに共通400ms制限がかかっていた問題を修正。連打3組で最短0.5秒の拘束に到達できる。本体モーションの制限は維持。108テスト、スマホ相当タッチの2操作モード成功。詳細はdocs/leaf-floors-pwa.md。


**v0.4.0（T-229）公開・検証済み:** 葉っぱのまもり、ひとやすみ床、とりもち床と脱出入力、げんき／のこりじかん表記、PWAを追加。承認画像・仕様は `docs/leaf-floors-pwa.md`。107テスト、4幅のブラウザ確認、オフライン・更新確認成功。アプリ141640d、正式URLで実行資材23件一致・オフライン起動を確認。公開結果はdeployment.mdに記録。


**最新v0.3.2公開・確認済み:** HP数値と被弾・回復の表示差分を統一（T-228）。整数が変わらない被弾は「微小」。内部HP・難易度は維持。アプリ61cc522、101テスト、公開先で実壁衝突3幅の表示一致を確認。

**最新v0.3.1公開・確認済み:** 初回操作SEの通信待ちとモード選択時の発音漏れを修正（T-227）。アプリ4bbef7a、97テスト、正式URLで初回4操作成功。BGMの開始条件は維持。

**最新v0.3.0公開・確認済み:** ユーザー指定のバージョン。チャレンジの見出し・難易度選択・開始を同じ枠にまとめた（T-226）。アプリ1036ce1、正式URLの4画面幅で操作確認済み。ゲームロジックはv0.2.12を維持。

**最新v0.2.12公開・確認済み:** アプリ `0ff92a9`。95テスト、公開先の4幅・センサー模擬検証成功。記録は初期閉の開閉式。次は新HPバランスと基準角度を実機試遊して調整。とりもち・床素材は未実装の将来案。

**v0.2.12:** チャレンジの最新仕様は [challenge-expansion.md](challenge-expansion.md)。やさしい・通常のHP係数、4枠の記録、回復キャンディ、面テーマ（ゴム壁を含む）、基準角度の保持を追加。以下の旧版記録・数値は履歴として参照。公開状況はdeployment.md。

**最新v0.2.11は正式URLで公開・動作確認済み:** 全画面のヘッダサイズと余白をプレイ画面基準へ統一。仕様は `ui-polish.md` T-224、公開状況は `deployment.md`。

**v0.2.10は正式URLで公開・動作確認済み:** ラン結果をクリーム色の立体パネルに統一。カウント前に通常0.6秒・コンティニュー1.5秒のREADYを追加。仕様は `results-and-timing.md`、公開状況は `deployment.md`。

**v0.2.9は正式URLで公開・動作確認済み:** BGMは毎回先頭から再生。停止位置を引き継がず、既存の待機時間とループ条件を維持。公開状況は `deployment.md`。

**v0.2.8は正式URLで公開・動作確認済み:** スタートSEが終わって0.2秒後にBGM開始（カウント終了から計1秒）。操作開始・SE・プレイ中だけの鳴動条件は維持。公開状況は `deployment.md`。

**v0.2.7は正式URLで公開・動作確認済み:** BGMはカウントダウン終了後のプレイ中だけ再生。カウントダウン・クリア・ゲームオーバー・プレイ以外では停止。SEは維持。公開状況は `deployment.md`。

**v0.2.6は正式URLへ公開済み:** ユーザー承認済みv2 BGMとSEを接続。ポーズ左の音切替、BGM/SE音量、停止・再開・保存を実装。検証中注記は維持。ヘッダA BIG FEELINGはロゴmの下端に揃える。仕様は `audio-implementation.md`。公開状況は `deployment.md`。

**v0.2.5は正式URLへ公開済み:** ヘッダの2段コピー・バージョン・注記下の余白をユーザー画像に合わせて調整。5画面幅のブラウザ確認成功。`ui-polish.md` のT-222、`deployment.md` を参照。

**v0.2.4は正式URLへ公開済み。** 公開コミット `8059402`、Pages成功。最新仕様は `ui-polish.md`、公開証跡は `deployment.md`。BGM・効果音は追加依頼を受けて `audio-proposal.md` に検討案のみ記録（未実装）。

**最新 v0.2.4:** 設定は迷路・HUDなしの独立画面。記録・ボタン・フォント・ロゴ配置・壁幅も更新。`ui-polish.md` を優先。以下は従来版の記録を含む。

## 現在地

**v0.2.3は正式URLに公開済み:** 迷路なしのモード選択、プラクティス優先、開始／再開前の3カウント、盤面上のポーズ、非モーダルトーストを実装。78テストと4ブラウザ検証が成功。正式URLでも新フローを4画面幅で確認済み。`start-flow.md` を最初に参照。公開証跡は `deployment.md`。

**v0.2.2は正式URLに公開済み:** C案・葉ロゴに加え、ユーザーの追加指示でトーストを中央配置、壁を同梱テクスチャへ差し替え、TIME/HPをトイ調に統一。序盤はクリアできると再試遊報告あり。78テスト成功、正式URLでも4画面幅のブラウザ検証成功。新しい描画・トースト・検証は `toy-visuals.md`、正式URLと公開証跡は `deployment.md`。以下はv0.2.1時点の引き継ぎ履歴。

**フェーズ2の実装・デプロイ・公開URLでの自動検証が完了。T-209の試遊で序盤の時間とHPが厳しいとの報告を受け、v0.2.1で緩和しました。再試遊が必要です。**
ローカルのバージョンは `v0.2.1`。フェーズ0は参照用のまま維持しています。

| 対象 | 状態 |
|---|---|
| フェーズ0・1 | 過去の実機確認・公開済み（従来資料の記録） |
| T-201〜204 / T-210 / T-212 / T-215〜216 | 引き継ぎ前のロジックを維持 |
| T-205〜207 / T-211 / T-213〜214 / T-217表示 | 実装済み、Chromeのブラウザ自動確認済み |
| T-209 | 試遊報告あり：序盤は時間不足・HPが減りすぎる。v0.2.1で緩和、再評価待ち |
| フェーズ2の公開 | **完了**。Git初期化、既存main履歴へ接続してpush。Pages成功・公開URLの動作確認済み。`docs/deployment.md` 参照 |

## 最初に読むもの

1. `AGENTS.md` — 特に§9の7原則
2. `docs/tasks.md` — 残タスク
3. `docs/run-and-score.md` — ラン・制限時間・スコア
4. `docs/hp-and-materials.md` — HPの根拠
5. `docs/phase2-implementation.md` と `docs/verification/README.md` — 今回の判断と検証

## 実装の入口

| ファイル | 責務 |
|---|---|
| `src/main.js` | 7画面の切り替え、入力・停止管理、ラン進行、記録保存の呼び出し |
| `src/game/stagePlay.js` | DOM非依存の1面。既存の迷路・素材・物理・HPを接続し、死亡→時間切れ→クリアを判定 |
| `src/game/run.js` | 面数・シード・コンティニュー。従来実装のまま |
| `src/game/progression.js` / `hp.js` | 難易度・HP。従来実装のまま |
| `src/ui/gameScreen.js` / `clearScreen.js` / `runScreens.js` | HUD、残HP、モード選択の記録、失敗・結果の表示 |
| `src/render/materialAppearance.js` / `canvasRenderer.js` | 素材の名前・色と矩形内の模様 |
| `src/record/storage.js` | 設定、練習タイム、ラン2系統のベスト保存 |

`createStagePlay(seed, difficulty)` は `stage / actor / hp / limitSec` と、
`timeMs / remainingSec / started / status` を持ちます。`advance({dt, elapsedMs, tilt, base})` は
物理の `onImpact` からHPを減らし、そのフレームのダメージを返します。
`difficulty` を省略するとHP・制限時間なし、壁はすべて標準です。

## 決めた動作

- 時間は数字＋バー、HPは数値＋バー。被弾時は背景色と減少値を450ms表示。
- チャレンジは速度0.5マス/s超、または開始位置から0.02マス移動で計測開始。
  微速で制限時間を回避できた問題を修正した。プラクティスの従来の速度判定は維持。
- 一時停止・設定・タブ非表示中は時間と物理を停止。設定やタブから戻っても手動で再開する。
  壁の凡例を開くと一時停止する。
- HPは面ごとに全回復。残HPはクリア時の評価表示であり、面数への加点はしない。
- スコアの「到達面数」はクリア済みの面数。UIでは「クリア面数」と明示する。
- 合計タイムはクリアした面だけを合算。失敗した試行の時間は含めない（既存runの契約）。
- 最初の失敗時にノーコン記録を保存し、その後のコンティニュー結果は別枠に保存。
  面数が多い方、同面数なら合計タイムが短い方を残す。
- プレイ中・クリア画面からもランを終了して結果を残せる。
- プラクティスはランダムシード、または `?seed=` を使用。同じ面のリトライと次の迷路を維持。
- 外周は標準壁。配置比率と物理は維持。v0.2.1では1面目の時間を2.5倍、通常ダメージを半分、単発上限を20%にし、10面目で従来値へ戻す。
  **ゴムは定義・描画対応済みだが、既存の素材配置では出現しない。** 追加配置する際はバランス検証が必要。

## 検証

```powershell
node --test
node tools/balance.mjs
python -m http.server 8765 --bind 127.0.0.1
```

ブラウザの再現スクリプトと実行手順は `docs/verification/README.md`。
`?debug=1` の `window.__corogalism` は従来の `teleport(x,y)` / `setTilt(x,y)` を維持し、
`state` に `screen / gameMode / stageIndex / hp / remainingSec / run / walls` を追加。
クエリなしではフックを公開しない。

## 次の実機試遊

1. 公開済みの `https://gerupon-lgtm.github.io/corogalism/` をPixel 6aとiPhone XRで開く。
2. 許可・拒否・キャリブレーションを確認する。
3. 両端末で素材の見分けやすさ、被弾表示、残り時間、停止・復帰、2回までのコンティニューを試す。
4. 「単調さが解消したか」「無理ゲーになっていないか」を判断し、T-209へ記録する。

本作の7原則、`[hidden]{display:none!important}`、セル単位の物理、指数減衰、
円×矩形衝突、`resolveParams`、HP閾値・単発上限、`noindex`・開発中表示を維持すること。
