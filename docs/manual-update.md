# T-233 手動更新チェック（v0.4.4）

トップの操作の設定の下に、既存のトイ調で「更新をチェック」を追加。
チェック中・最新版・更新あり・通信失敗／オフライン・非対応を表示する。
チェックだけでは再読み込みしない。更新ありは既存のタイトル上部「更新する」へ案内。
ダウンロード完了を待ってから結果を表示し、確認は15秒でタイムアウト。連打中は無効化。
プレイ中の自動更新をせず、別タブ保護・明示更新・記録と設定の維持は既存どおり。
