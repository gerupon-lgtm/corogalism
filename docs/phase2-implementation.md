# フェーズ2 UI実装の作業記録（2026-09-12）

正典: `tasks.md`、`run-and-score.md`、`hp-and-materials.md`。

## 計画

- [x] T-211: モード選択とプラクティス維持
- [x] T-205 / T-206 / T-217: HP・残り時間・面進行の接続
- [x] T-207: 壁素材の色と模様、凡例
- [x] T-213: 失敗画面、同一面のコンティニュー
- [x] T-214: ラン結果と2系統の記録保存
- [x] 自動テスト、ブラウザ確認、設計書・引き継ぎ更新
- [ ] T-209: Pixel 6a / iPhone XRでの試遊（発案者が実施）

## 判断と接続契約

| 対象 | 確認・判断 |
|---|---|
| T-211 / T-205 / T-217 | プラクティスはHP・制限時間なし、壁はdefaultのみ。チャレンジだけ既存のdifficulty/hp/runを使う |
| T-205 / T-207 | 衝突通知onImpactだけでダメージを処理し、HPバーの減少と被弾表示へ接続する |
| T-206 / T-213 | HP0・時間切れをゴールより優先する。クリア時の残HPは評価表示であり面数スコアには加算しない |
| T-213 / T-214 | 最初の失敗時にノーコン記録を確定し、コンティニュー後の結果を別枠へ保存する |
| 合計タイム | 既存run.clearStageの契約に従い、クリアした面のタイムだけ合計する |
| 時間 | 動き始めから計測。一時停止・設定・非表示中は除外。復帰時は手動再開 |
| 表示裁量 | 残り時間は数字＋バー。折れ回数は非表示。ノーミス面数を結果に表示 |
| 終了導線 | 途中でもランを終了でき、クリア済みの面数を記録する。プラクティスはモード選択へ戻れる |
| 作業環境 | 配布フォルダに.gitがないため直接編集。コミット・pushは未実施 |

## 検証記録

- 変更前: `node --test` 60件成功。
- 変更後: `node --test` 73件成功（保存6件・面進行7件追加）。
- Chromeブラウザ確認: 基本導線と異常系の2スクリプトが成功。例外0件。画像と手順は `verification/README.md`。
- レビュー: 微速で時計が始まらない問題を再現し、開始位置から0.02マス移動でも計測するよう修正。
  回帰テストは修正前に失敗、修正後に成功。再レビューの追加指摘なし。
- Windowsで `tools/balance.mjs` の起動判定が不一致となり出力されない問題を修正。
  ファイルURLの比較にNode標準の `pathToFileURL` を使用。測定処理は変更しない。
