# コロガリズム / Corogalism — 実装指示（フェーズ2）

このファイルは実装エージェント向けの指示です。中身は `CLAUDE.md`・`GEMINI.md` と共通です。

## 1. 概要

端末の傾き（`DeviceOrientationEvent`）で盤面ごと傾け、転がるキャラクターを迷路のゴールへ導くクリア型ブラウザゲーム。迷路は1画面に収まりきるサイズで、盤面全体を見ながら遊ぶ。傾きセンサーのない端末では「画面のタッチ／クリック位置で擬似的に傾ける」モードで同じ面が遊べる。

生成AI（LLM）は一切使わない。迷路は DFSバックトラッカー＋BFS到達可能性検証というブラウザ内の決定的アルゴリズムで生成する。

姉妹プロジェクト「迷路脱出ゲーム」「AmazingMaze」は設計とPythonプロトタイプまでで本実装が未着手のため、**本プロジェクトが最初のJavaScript実装**であり、ここで書く迷路生成モジュールが後続2作の土台になる。生成モジュールはこのゲームの都合から独立させる。

対象ユーザーは現時点では開発者本人（シクミラボ / SIKUMI LAB の作品として外部公開はするが、フェーズ2までポートフォリオからリンクしない）。

## 2. 現在の状態

**v0.4.5（T-234）:** トップにあそびのガイドを追加。壁・アイテム・床を1枚のオーバーレイに掲載。とりもち説明を指定の一文へ要約し、ひとやすみと等高。仕様docs/play-guide.md。

**v0.4.4（T-233）公開確認済み・78b4c39:** トップの設定下に手動更新チェックを追加。最新版・更新あり・通信失敗を表示し、更新適用は既存の明示操作。詳細docs/manual-update.md。

**v0.4.3（T-232）公開確認済み・9624df3:** やさしい9面から制限時間に段階的に最大12秒加算。木枠の砂時計を9面以降に抽選配置、取得+5秒。ゴム壁の実効反発を0.7へ強化。仕様はdocs/time-and-hourglass.md。

**v0.4.2（T-231）公開確認済み・1f00540:** とりもちを自然脱出3秒、ダブルタップ1組につき0.5秒短縮、最短0.5秒へ調整（下限到達には5組）。盤面内の2タップ位置合わせを不要にし、指の移動許容は48px。ヒントは球を避けて配置し、短縮成功を明示。109テストと実タッチ模擬・3画面幅を確認。詳細はdocs/leaf-floors-pwa.md。

**v0.4.1（T-230）:** 画面のダブルタップに共通400ms制限がかかっていた問題を修正。連打3組で最短0.5秒の拘束に到達できる。本体モーションの制限は維持。108テスト、スマホ相当タッチの2操作モード成功。詳細はdocs/leaf-floors-pwa.md。

**v0.4.0（T-229）公開・検証済み:** 葉っぱのまもり、ひとやすみ床、とりもち床と脱出入力、げんき／のこりじかん表記、PWAを追加。承認画像・仕様は `docs/leaf-floors-pwa.md`。107テスト、4幅のブラウザ確認、オフライン・更新確認成功。アプリ141640d、正式URLで実行資材23件一致・オフライン起動を確認。公開結果はdeployment.mdに記録。


**v0.3.2更新:** HP数値と被弾・回復の増減表示を共通の丸め基準へ統一。整数が変わらない被弾は「微小」と表示。内部HPは維持。`docs/ui-polish.md` T-228参照。

**v0.3.1更新:** 初回操作SEの読み込み待ちと、初回モード選択時の無効化による発音漏れを修正。`docs/audio-implementation.md` T-227参照。

**v0.3.0更新:** ユーザー指定のバージョン。トップのチャレンジ枠内に難易度選択・説明・開始ボタンをまとめた。`docs/ui-polish.md` T-226参照。

**v0.2.12更新:** やさしいモード・難易度別記録・回復キャンディ・面テーマとゴム壁・安定姿勢による基準設定。`docs/challenge-expansion.md` を参照。床素材ととりもちは将来案。

**v0.2.11更新:** 全画面のヘッダをプレイ画面の既存サイズ・余白へ統一。盤面・操作ボタンの配置は維持。`docs/ui-polish.md` T-224参照。

**v0.2.10更新:** ラン結果をトイ調に統一。開始・再開前に0.6秒、コンティニュー前は1.5秒のREADYを追加。`docs/results-and-timing.md` 参照。

**v0.2.9更新:** BGMは再開時も毎回曲の先頭から再生。スタートSE後0.2秒の間、プレイ中だけの再生、ループを維持。`docs/audio-implementation.md` 参照。

**v0.2.8更新:** 承認済みタイミングに合わせ、スタートSE0.8秒＋間0.2秒の後にBGM開始。操作開始は従来どおり。`docs/audio-implementation.md` 参照。

**v0.2.7更新:** BGMをカウントダウン終了後のプレイ中だけに制限。その他の画面では停止し、SEは維持。`docs/audio-implementation.md` 参照。

**v0.2.6更新:** 承認済みBGM・SE、ポーズ左の音切替、独立音量設定を実装。検証中注記を維持し、ヘッダの最終行をロゴmの下端へ揃えた。`docs/audio-implementation.md` を参照。

**v0.2.5更新:** ロゴ右コピー・バージョンの位置と注記下の余白を調整。`docs/ui-polish.md` のT-222を参照。

**v0.2.4更新:** タイトルの読点除去、ロゴ右に2段コピー、明るい記録カード、操作ボタンと独立設定のトイ調、同梱M PLUS 1p、壁幅0.24セルを実装。79テスト成功。最新仕様は `docs/ui-polish.md`。

**v0.2.3更新:** タイトルは迷路なしのモード選択、プラクティス優先。開始／再開前に3カウント、盤面上にポーズ。トースト表示中も設定・終了を操作可能。最新の動作と検証は `docs/start-flow.md` を参照。

**v0.2.2更新:** C案のトースト・光沢球・カップ型ゴール・素材壁・葉ロゴを実装。78テスト成功。4画面幅で終了／再開時のスクロール不変を検証。`docs/toy-visuals.md` と `docs/deployment.md` を参照。以下のv0.2.1記録は難易度調整時点の履歴。

**フェーズ0（操作感の検証）は合格済み**（2026-09-11、合格ライン a〜e 全通過。Pixel 6a と iPhone XR の実機で確認）。

既にあるもの:

- `phase0/index.html` — フェーズ0のプロトタイプ（単一ファイル）。傾き入力・iOS許可・キャリブレーション・擬似傾きモード・転がり物理・7×7迷路生成が動く状態
- 実機で調整した基準値（`docs/requirements.md` §3、`gameConfig.js` の初期値になる）

**フェーズ1は完了しています**（2026-09-12、実機確認・デプロイ済み）。`node --test` で30件すべて成功する状態。
所見は「十分遊べるが簡単すぎる」で、**フェーズ2はHPと壁の素材で手応えを足す作業**です。

**フェーズ2のローカル実装は完了**（v0.2.1、2026-09-12）。`node --test` は76件成功。Chromeのブラウザ自動確認済み。Git初期化と既存履歴への接続を行い、mainへのpush・GitHub Pages公開まで完了しました。公開URLでChromeの自動確認も成功しています。残りはT-209の実機試遊です。現在地は `docs/handoff-codex.md` を参照してください。

フェーズ2の数値の根拠・検証データ・素材テーブルは **`docs/hp-and-materials.md` が正典**です。
実装前に必ず読んでください。閾値と1発の上限には、外すと壊れる具体的な理由（実測値）が書いてあります。

## 3. フェーズ2のスコープ

### やること

- **経路と折れ回数の算出**（`maze/path.js`。後続2作でも使える独立モジュールとして書く）
- **HP**（初期値は折れ回数でスケール、ダメージは衝突速度の二乗、閾値と1発の上限つき）
- **壁の素材**（反発係数とダメージ係数を別々に振る。5種類をデータで追加）
- HP表示、HP0の扱い、残HPのボーナス
- 素材の描画（**見えないと理不尽になるので手を抜かない**）
- **バランス検証テスト**（丁寧に走れば死なないことを機械的に保証する）

### やらないこと（フェーズ3以降）

- 床素材（氷・砂などの摩擦系）／穴／コンベア — 同じフェーズでやるかは【要確認】
- キャラクターの追加（構造は既にある。キャラは1体のまま）
- デイリー／シード共有UI、スター評価・実績、キャラ解禁
- カメラ追従（画面より大きい迷路）
- 難易度上昇カーブ（迷路は 7×7 固定。**マスを細かくする方向は操作精度の限界にすぐ当たるため採らない**）
- 鍵と扉・スイッチとゲートのような順序依存ギミック（企画として見送り済み）

## 4. 技術スタック

- 言語: Vanilla JavaScript（ES2020+、ES modules）、HTML5 Canvas 2D
- ビルド: なし（プレーンJS）。GitHub Pages にそのまま置ける形を優先する
- ホスティング: GitHub Pages（`gerupon-lgtm/corogalism`、公開URL `https://corogalism.sikumilab.com/`）
- データ永続化: `localStorage`（自己ベスト・設定のみ）
- テスト: Node標準の `node --test`（純粋関数のみ対象）
- バージョン: セマンティックバージョニング、接頭辞 `v`、フェーズ1の初期値 `v0.1.0`

## 5. ディレクトリ構成

```
/
├── index.html                    # エントリーポイント（画面はDOM/Canvasの切り替えで表現）
├── style.css
├── phase0/
│   └── index.html                # フェーズ0プロトタイプ（参照用に残す。触らない）
├── src/
│   ├── main.js                   # 初期化・画面遷移・ゲームループ
│   ├── config/
│   │   └── gameConfig.js         # 基準値（baseline）とクランプ範囲を一元管理
│   ├── maze/
│   │   ├── rng.js                # シード付き乱数（xorshift32）
│   │   ├── generator.js          # DFSバックトラッカー（サイズとシードを引数に取る純粋関数）
│   │   └── validator.js          # BFS到達可能性検証
│   ├── physics/
│   │   ├── resolveParams.js      # ★層構造の解決とクランプ（docs/physics.md）
│   │   ├── integrator.js         # 固定タイムステップ積分・力の集約
│   │   └── collision.js          # 円 × 矩形の衝突解決
│   ├── input/
│   │   ├── tiltSource.js         # DeviceOrientation・iOS許可・キャリブレーション
│   │   ├── pointerSource.js      # 擬似傾き（タッチ／クリック位置）
│   │   └── tiltVector.js         # 2モードを共通の傾きベクトルへ正規化・平滑化
│   ├── world/
│   │   ├── materials.js          # 素材定義（5種。ゴムは「はずむ迷路」で出現）
│   │   ├── characters.js         # キャラ定義（default のみ）
│   │   └── stage.js              # 迷路＋素材配置＋ゾーンをまとめた1面
│   ├── render/
│   │   ├── camera.js             # ★カメラ抽象（fixed実装のみ）
│   │   └── canvasRenderer.js     # 描画
│   ├── record/
│   │   └── storage.js            # localStorage（自己ベスト・設定）
│   └── ui/
│       ├── titleScreen.js        # S-101
│       ├── gameScreen.js         # S-102
│       ├── clearScreen.js        # S-103
│       └── settingsScreen.js     # S-104
├── test/                         # node --test 対象
└── docs/                         # 本設計書一式
```

## 6. 規約

- 迷路生成・到達可能性検証・`resolveParams` は **DOM操作と描画から独立した純粋関数**として実装し、単体テストを書く
- ゲーム設定値（基準値・クランプ範囲）は `src/config/gameConfig.js` に集約する。コード各所にマジックナンバーを埋めない
- **物理パラメータは必ず `resolveParams` 経由で取得する。** `gameConfig` の値を物理コードから直接読まない（`docs/physics.md` 参照）
- 速度・加速度・サイズは**マス（セル）単位**で保持し、描画時にピクセルへ変換する。画面サイズで難易度が変わらないようにする
- 機能実装時は `docs/tasks.md` のタスクIDをコミットメッセージに含める（例: `feat(T-106): resolveParamsとクランプ`）
- コメントと識別子は日本語コメント＋英語識別子。姉妹プロジェクトと揃える

## 7. コマンド

- 開発（擬似傾きモードの確認）: `npx serve` などで配信し `http://localhost:PORT` を開く
- テスト: `node --test`
- 実機確認（傾きモード）: `main` に push して GitHub Pages で確認する
- 開発用フック: `?debug=1` を付けると `window.__corogalism`（`state` / `teleport(x,y)` / `setTilt(x,y)`）が生える。ブラウザでの自動確認に使う。クエリが無ければ何も生えない

**傾きセンサーはセキュアコンテキスト（HTTPS）でしか動きません。** 開発機のLAN IP（`http://192.168.x.x`）にスマホから繋いでもセンサーは反応しません（`localhost` は例外）。**PC側は `localhost` で擬似傾きモードを使って進め、傾きモードの確認だけデプロイして実機で行う**二段構えにしてください。

## 8. 実装上の注意

- **フェーズ0の挙動を壊さない。** 再編後に、同じ基準値で同じ手触りになっているかを実機で確認する（T-116）。プロトタイプは `phase0/` に残してあるので、迷ったら挙動を比較する
- **`friction` はクーロン摩擦ではなく指数減衰の係数**。`v *= Math.exp(-friction * dt)` として適用する。物理的に正しい摩擦モデルに「直さない」こと（フェーズ0で実機調整した値がこのモデル前提）
- **衝突はセル単位のクランプではなく、円×矩形で解決する。** セル単位のクランプだと角で壁を抜ける。壁は矩形の配列として持ち、円との最近点で押し出し＋法線方向の速度反射を行う
- **トンネリング対策を残す。** 速度上限とサブステップ分割（フェーズ0で実装済み）を再編後も維持する
- **iOSの許可が拒否された場合、センサー非搭載の場合、センサー値が届かない場合は、必ず擬似傾きモードへ着地させる。** ここで行き止まりを作らないことはフェーズ0で満たした要件なので、後退させない
- **キャリブレーションはプレイ中も実行できるようにする。** 基準がずれたまま操作不能になるのを避けるため
- 縦持ち前提。横向き時は警告を出すだけでよい（フェーズ1のスコープ）
- `localStorage` の読み書きは必ず try/catch で囲み、読めなくても動くようにする（プライベートウィンドウ等）

## 9. 判断の裁量

**発案者（gerupon）の指示で覆してよいもの**（相談も不要。指示があればそのまま従ってください）:

- 画面のレイアウト・配置・色・フォント・文言・アニメーション
- ボタンやスライダーの並び、設定画面の項目の並び
- `gameConfig.js` の数値（基準値・クランプ範囲の具体値）
- ファイル分割の粒度、モジュール名、関数名
- 画面の数や遷移の順序（`docs/screens.md` は目安です）
- テストの書き方・粒度

**指示があっても覆さないもの**（根拠つき。覆す必要が出たら、実装を止めて発案者に確認してください）:

1. **物理パラメータを `resolveParams` 経由にする原則**（`docs/physics.md`）
   → 根拠: 摩擦・反発・ボールサイズは素材や重力やキャラで変わる要素にする方針が確定しており、後から層構造へ変えると物理コアの構造変更＋バランス再調整が発生する。係数が全部 1.0 の今なら、通すだけでコストがほぼゼロ。**「今は素材が1種類だから直接読んでも同じ」は、まさに避けたい近道です**
2. **迷路生成の到達可能性検証（BFS）を必ず通すこと**
   → 根拠: 「破綻しない迷路生成」がこのプロジェクト群の技術検証テーマそのもの。生成後の検証を省くと、ゴールへ到達できない面が出る可能性を放置することになる
3. **生成AI（LLM）・外部APIを使わないこと**
   → 根拠: 「LLMを使わず決定的アルゴリズムだけで成立させる」ことが企画の核。名前（コロガリズム＝転がる＋アルゴリズム）もそこから来ている
4. **迷路を1画面に収める（フェーズ1ではカメラを動かさない）こと**
   → 根拠: 実寸比較で操作可能な下限マス数を確認済み。カメラを動かす対応はフェーズ4。ただし**カメラを固定値としてコードに埋め込まず、差し替え可能な抽象にしておく**こと
5. **バックエンドを持たないこと／個人情報を扱わないこと**
   → 根拠: 静的サイトのみで成立する設計。スコアの外部送信もフェーズ1では行わない
6. **HPのダメージ閾値（1.2マス/s）と1発の上限（初期HPの35%）を外さないこと**
   → 根拠: どちらも実測で必要性が確認されている。閾値が無いと**壁への押し付けで3秒に254回の微小接触が全部ダメージになる**（閾値ありなら2回）。上限が無いと**全力衝突（5.38マス/s）で1発即死する**（初期HP100に対して175ダメージ）。数値自体は調整してよいが、**仕組みごと外さないこと**。詳細は `docs/hp-and-materials.md` §3
7. **`noindex` と「開発中」注記を外さないこと**
   → 根拠: フェーズ2まではポートフォリオからリンクしない運用のため。外すのはフェーズ3の判断

上の1〜7以外で迷った場合は、**止めずに素直な実装で進め、判断した内容をコミットメッセージか報告に1行書いてください。** 過去に禁止事項が多すぎて実装が止まった経験があるため、この指示書は「止める条件」を意図的に7件に絞っています。

## 10. 引き継ぎ

**Codexが引き継ぐ場合は `docs/handoff-codex.md` を最初に読んでください。**
現在地・残タスク・すでにある部品の呼び方・詰まりやすいところがまとまっています。

## 11. 参照

- 構想と経緯・決定ログ: `../構想たたき台_傾き操作迷路ゲーム.md`（親フォルダ。決定の根拠はここが正典）
- 要件（機能IDの正典）: `docs/requirements.md`
- 物理パラメータの契約: `docs/physics.md` ← フェーズ1の中核
- **HPと壁の素材: `docs/hp-and-materials.md` ← フェーズ2の中核。数値の根拠と検証データ**
- バランス検証コード: `tools/`（数値を変えたら回して生存率と難易度勾配を確認できる）
- データモデル: `docs/data-model.md`
- 画面: `docs/screens.md`
- タスク: `docs/tasks.md`
- フェーズ0プロトタイプ: `phase0/index.html`
